import { LayoutShowcase } from "@/components/demo-components"

export default function LayoutTestPage() {
  return <LayoutShowcase />
}

export const metadata = {
  title: "Layout Test - Spamsense", 
  description: "Testing the complete dashboard layout with sidebar and top bar",
}
